---
import Layout from '../layouts/Layout.astro';
---



<Layout title="aboutUs">
    <head>
        <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Carter+One&display=swap">
		<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Almendra&display=swap">
		<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Abel&display=swap">
    </head>
    <section>
        <script>
			// Number of images you want to display
			const numImages = 30;
		
			// The path to your image
			const imageUrl = '../stars.png';
		
			// The container where the images will be placed
			const container = document.getElementById('random-images-container');
			if (!container) throw new Error("Images container not found");
		
			// Get the heights of the nav and footer
			const nav = document.querySelector('nav');
			const footer = document.querySelector('footer');
			if (!nav || !footer) throw new Error("Nav or footer not found");
		
			const navHeight = nav.offsetHeight;
			const footerHeight = footer.offsetHeight;
		
			for (let i = 0; i < numImages; i++) {
				// Create a new image
				const img = new Image();
				img.src = imageUrl;
				img.style.position = 'absolute';
		
				// Change the size of the image
				img.style.width = '30px';  // Change this to the width you desire
				img.style.height = '30px'; // Change this to the height you desire
		
				// Generate a random position for the image
				img.style.top = Math.random() * (document.body.scrollHeight - navHeight - footerHeight) + navHeight + 'px';
				img.style.left = Math.random() * document.body.scrollWidth + 'px';
		
				// Add the image to the container
				container.appendChild(img);
			}
		</script>
        <div class="aboutUs">
            <div class="aboutUs__header">
                <h1>About MapIT</h1>

                <p  class="aboutUs__text_first">Welcome to MapIT – the ultimate app for exploring and sharing your favorite places with friends! MapIT is designed to revolutionize the way you navigate and interact with the world around you. Whether you're discovering hidden gems, planning adventures, or simply sharing your favorite spots with friends, MapIT is your go-to companion.</p>
            </div>
            <div class="aboutUs__main">
                <div class="aboutUs__content">
                    <h2 class="aboutUs__subTitles">Our Mission</h2>

                    <p class="aboutUs__text">At MapIT, our mission is simple: to connect people through shared experiences and the places they love. We believe that every corner of the world has something special to offer, and we're here to help you uncover and share those treasures with the people who matter most to you.</p>

                    <h2 class="aboutUs__subTitles">Explore, Discover, Share</h2>

                    <p class="aboutUs__text">With MapIT, the world is yours to explore. Simply drop a marker on any place that catches your eye – whether it's a cozy café, a breathtaking viewpoint, or a hidden gem off the beaten path. Add a photo, a title, and a brief description to make your marker come to life. Then, watch as it's instantly shared with all the friends you've connected with on the app.</p>

                    <h2 class="aboutUs__subTitles">Connect with Friends</h2>

                    <p class="aboutUs__text">MapIT isn't just about discovering new places – it's about sharing those discoveries with the people you care about. Connect with friends on MapIT to see the places they've marked, and let them in on your own favorite spots. Whether you're planning a group outing, sharing travel tips, or simply reminiscing about past adventures, MapIT makes it easy to stay connected and inspired.</p>

                    <h2 class="aboutUs__subTitles">Privacy and Security</h2>

                    <p class="aboutUs__text">At MapIT, we prioritize your privacy and security. You have full control over who sees your markers and who you connect with on the app. We're committed to creating a safe and welcoming community where you can freely share your experiences without compromising your privacy.</p>
            
                </div>
                <div class="aboutUs__images">
                    <img class="aboutUs__image" src="../about us mapit.jpeg" alt="About Us" />

                    <img class="aboutUs__image" src="../aboutUs--mapit.jpeg" alt="About Us" />
                    <div id="add_image_for_phone">
                        <script>
                            window.addEventListener('resize', function() {
                                const container = document.getElementById('add_image_for_phone');
                                if (container) {
                                    if (window.innerWidth < 900) {
                                        // Add the image only if it doesn't already exist
                                        if (!container.querySelector('img')) {
                                            const img = document.createElement('img');
                                            img.src = '../engoy-mapit.png'; // Change this to your image path
                                            img.style.width = '100%';
                                            img.style.marginLeft = '0px';
                                            img.style.padding = '0px';
                                            img.style.borderRadius = '25px';
                                            img.style.marginTop = '300px';
                                            img.style.position = 'relative'; // Add this
                                            img.style.zIndex = '1'; // Add this
                                            container.appendChild(img);
                                        }
                                    } else {
                                            // Remove the image if the window is wider than 900px
                                            const img = container.querySelector('img');
                                            if (img) {
                                                    container.removeChild(img);
                                            }
                                        }
                                }
                            });

                            // Run the function once the page is loaded
                            window.dispatchEvent(new Event('resize'));
                        </script>
                    </div>
                </div>
            </div>
        </div>
    </section>
</Layout>

<style>
    section {
        display: flex;
        justify-content: center;
        align-items: center;
        flex-direction: column;
        padding-left: 20px;
        padding-right: 20px;
    }

    h1 {
        font-family: 'Carter One', cursive;
        font-size: 3rem;
        margin-bottom: 20px;
        text-align: left;
    }


    @keyframes fadeIn {
        0% {opacity: 0;}
        100% {opacity: 1;}
    }

    
    .aboutUs__header{
        animation: fadeIn 2s;
        align-items: center;
        display: flex;
        justify-content: center;
        flex-direction: column;
        margin: 0 auto;
        max-width: 48%; /* o cualquier otro valor que prefieras */
        width: 100%;
        padding-bottom: 50px;
    }

    .aboutUs__main {
        display: flex;
        justify-content: center; /* Centra el contenido horizontalmente */
        padding-left: 150px;
    }

    .aboutUs__content {
        flex: 1.5;
        background: white; /* Hace que el fondo sea blanco */
        box-shadow: 0px 0px 5px #980000; /* Añade una sombra */
        width: 30px; /* Ajusta el ancho */
        margin-left: 50px; /* Añade espacio a la izquierda del cuadrado */
        margin-bottom: 50px; /* Añade espacio debajo del cuadrado */
        padding-bottom: 30px; /* Añade espacio debajo del contenido */
        position: relative; /* Añade esto */
        z-index: 2; /* Añade esto */
    }

    .aboutUs__images {
        flex: 1.5; /* Aumenta el tamaño */
        position: relative; /* Añade esto */
        z-index: 0; /* Añade esto */
        margin-top: -50px; /* Añade esto */
        margin-left: -50px;
    }

    .aboutUs__image {
        width: 600px;
        margin-left: 0px;
        box-shadow: 0px 0px 5px #980000; /* Añade una sombra */
        padding: 0.5px; /* Añade espacio alrededor del contenido */
        border-radius: 25px; /* Añade esto */
    }

    .aboutUs__image:nth-child(2) {
        position: absolute; /* Añade esto */
        top: 430px; /* Añade esto */
        left: -50px; /* Añade esto */
    }

    .aboutUs__subTitles {
        font-family: 'Carter One', cursive;
        font-size: 2rem;
        margin-bottom: 10px;
        text-align: left;
        text-indent: 50px;
        animation: fadeIn 4s;
    }
    .aboutUs__text_first{
        font-family: 'Abel', sans-serif;
        font-size: 1.3rem;
        margin-bottom: 20px;
        text-align: justify;

    }
    .aboutUs__text {
        font-family: 'Abel', sans-serif;
        font-size: 1.3rem;
        margin-bottom: 20px;
        text-align: justify;
        max-width: 60%; /* Añade esto */
        margin: 0 auto; /* Añade esto */
    }

    #random-images-container {
        position: absolute;
        z-index: -1; /* Coloca el contenedor de imágenes detrás de otros contenidos */
        width: 100%;
        height: 100%;
	}


    @media (max-width: 900px) {

        .aboutUs__main {
            padding-left: 20px; /* Ajusta el padding para pantallas pequeñas */
        }

        .aboutUs__header{
            animation: fadeIn 2s;
            align-items: center;
            display: flex;
            justify-content: center;
            flex-direction: column;
            margin: 0 auto;
            max-width: 70%; /* o cualquier otro valor que prefieras */
            width: 100%;
            padding-bottom: 20px;
        }
        

        .aboutUs__content {
            flex: 1;
            margin-top: 30px;
            margin-bottom: 100px;
            max-width: 900px;
            padding-left: 10px;
            position: relative; /* Añade esto */
            z-index: 1; /* Añade esto */
        }

        .aboutUs__text {
            font-family: 'Abel', sans-serif;
            font-size: 1.3rem;
            margin-bottom: 20px;
            text-align: justify;
            max-width: 80%; /* Añade esto */
            margin: 0 auto; /* Añade esto */
        }

        .aboutUs__subTitles {
            font-family: 'Carter One', cursive;
            font-size: 2rem;
            margin-bottom: 10px;

            text-align: left;
            text-indent: 13px;
            animation: fadeIn 4s;
        }

        .aboutUs__images {
            flex: 1;
            margin-top: 0.5px;
            margin-left: -10;
            margin-bottom: 50px;
            position: relative; /* Añade esto */
            z-index: 0; /* Añade esto */
        }

        .aboutUs__image {
            width: 100%;

        }
        .aboutUs__image:nth-child(2) {
            position: absolute; /* Añade esto */
            top: 350px; /* Añade esto */
            left: -20px; /* Añade esto */
        }
    }
</style>